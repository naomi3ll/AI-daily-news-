# AI Daily News - 全自动工作流
# 功能：每天16:00自动采集 + 自动发布网页

name: Daily AI News

on:
  schedule:
    - cron: '0 8 * * *'  # 北京时间16:00
  workflow_dispatch:     # 手动触发
  push:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 安装依赖
        run: pip install -r requirements.txt

      - name: 🚀 采集新闻
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python src/main.py --demo
        continue-on-error: true

      - name: 📤 提交数据更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --staged --quiet; then
            echo "无数据更新"
          else
            git commit -m "📰 更新 $(TZ='Asia/Shanghai' date +'%Y-%m-%d') 数据"
            git push
          fi

      - name: 📁 准备部署文件
        run: |
          mkdir -p _site
          cp index.html _site/
          cp -r css _site/
          cp -r js _site/
          cp -r data _site/
          echo "部署文件准备完成"
          ls -la _site/

      - name: 📦 配置Pages
        uses: actions/configure-pages@v4

      - name: 📤 上传artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: 🚀 部署到Pages
        id: deployment
        uses: actions/deploy-pages@v4
